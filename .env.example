# =============================================================================
# App Factory Template - Environment Variables
# =============================================================================
# 
# Copy this file to .env and fill in your actual values.
# All services are optional - the app will gracefully handle missing config.
#
# Quick Start:
# 1. Copy: cp .env.example .env
# 2. Fill in the services you want to use (see instructions below)
# 3. Run: npm run dev
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL via Drizzle ORM)
# -----------------------------------------------------------------------------
# Required for user management and data persistence.
# Get a free database at: https://neon.tech or https://supabase.com
#
# Example: postgresql://user:password@host:5432/database
DATABASE_URL=

# -----------------------------------------------------------------------------
# Clerk Authentication
# -----------------------------------------------------------------------------
# Required for user authentication (sign-in, sign-up, user management).
# Get your keys at: https://dashboard.clerk.com
#
# 1. Create an account at https://clerk.com
# 2. Create a new application
# 3. Copy the publishable and secret keys from the API Keys page
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Optional: Required only if you want to sync user events via webhooks
# 1. Go to Webhooks in Clerk dashboard
# 2. Add endpoint: https://yourdomain.com/api/webhooks/clerk
# 3. Subscribe to: user.created, user.updated, user.deleted
# 4. Copy the signing secret
CLERK_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Upstash Redis (Cache + Rate Limiting)
# -----------------------------------------------------------------------------
# Required for caching and rate limiting features.
# Get a free database at: https://upstash.com
#
# 1. Create an account at https://upstash.com
# 2. Create a Redis database
# 3. Copy the REST URL and token from the database details page
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# -----------------------------------------------------------------------------
# Cloudinary (Media Upload)
# -----------------------------------------------------------------------------
# Required for image upload functionality.
# Get a free account at: https://cloudinary.com
#
# 1. Create an account at https://cloudinary.com
# 2. Go to Dashboard
# 3. Copy Cloud Name, API Key, and API Secret
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# -----------------------------------------------------------------------------
# Optional: Application Settings
# -----------------------------------------------------------------------------
# Base URL for your application (used for redirects and webhooks)
# Default: http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
